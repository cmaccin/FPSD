@model IEnumerable<FPSD.Models.Sport>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SportsType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SportsName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SportsType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SportsName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SportsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SportsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SportsID })
        </td>
    </tr>
}

</table>
